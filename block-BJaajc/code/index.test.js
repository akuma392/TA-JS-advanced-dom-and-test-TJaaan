// const getFullName = require('./getFullName');

// test('Abhi and kum  will be Abhi Kum', () => {
//   expect(getFullName('Arya', 'Stark')).toBe('Arya Stark');
// });

const palindrome = require('./palindrome');

test('12321 will be Palindrome', () => {
  expect(palindrome(12321)).toBeTruthy();
});

// test('123321 will be Palindrome', () => {
//   expect(palindrome(123321)).not.toBeTruthy();
// });

// const getCircumfrence = require('./getCircumfrence');

// test('Circumference of 5 is 31.4', () => {
//   expect(getCircumfrence(5)).toEqual(`The area is 31.4`);
// });

// // const getArea = require('./getArea');

// const average = require('./average');

// test('AVerage of 10 and 20 is 15', () => {
//   expect(average(10, 20)).toBe(15);
// });

// test('AVerage of 30 and 20 is not 15', () => {
//   expect(average(30, 20)).not.toBe(15);
// });

// test('AVerage of 20 and 20 is greater than 15', () => {
//   expect(average(20, 20)).toBeGreaterThan(15);
// });
// test('AVerage of 30 and 20 is greater than 15', () => {
//   expect(average(30, 20)).toBeLessThan(40);
// });
// test('AVerage of 10 and 10 is 10', () => {
//   expect(average(10, 10)).toBe(10);
// });

// // const longestWord = require('./longestWord');
